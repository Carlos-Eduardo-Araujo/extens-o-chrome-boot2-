services:
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    container_name: bootcamp-api

  web:
    build: ./apps/web
    ports:
      - "8080:80"
    depends_on:
      - api
    container_name: bootcamp-web
