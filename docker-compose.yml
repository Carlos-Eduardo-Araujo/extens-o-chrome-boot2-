services:
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    container_name: bootcamp-api

  web:
    build: ./apps/web
    ports:
      - "8080:80"
    container_name: bootcamp-web
    depends_on:
      - api
